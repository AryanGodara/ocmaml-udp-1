UDP Client and Server implmented in OCaml

# To run the server
```bash
cd udpServer
dune build
./_build/default/bin/main.exe
```

# To run the Client
```bash
cd udpClient
dune build
./_build/default/bin/main.exe args
```

New functionality is being added; stay tuned :)